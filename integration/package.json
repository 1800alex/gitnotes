{
  "name": "gitnotes-integration",
  "private": true,
  "description": "End-to-end integration tests: dockerized app + seeded repo + Playwright.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
